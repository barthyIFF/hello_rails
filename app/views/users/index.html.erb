<ul>
<% @users.each do |user| %>
  <li>
    <%= link_to user.email, edit_user_path(user)   %>
   -  <%= link_to 'DELETAR', @user, method: :delete, data: { confirm: 'Vc está certo de que quer excluir?' } %>
  </li>  
<% end %>
</ul>
